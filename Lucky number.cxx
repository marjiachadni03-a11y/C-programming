#include <stdio.h>

int main() {
    int luckyNumber;

    printf("Type your lucky number: ");
    scanf("%d", &luckyNumber); // Takes input from user

    printf("Your lucky number is %d!\n", luckyNumber);
    return 0;
}