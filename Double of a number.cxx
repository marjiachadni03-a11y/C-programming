#include <stdio.h>
int main() {
    int number, result;
    printf("Enter any number: ");
    scanf("%d", &number);
    result = number * 2;
    printf("Double of %d is %d.\n", number, result);
    return 0;
}}